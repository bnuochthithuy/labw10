apiVersion: v1
kind: Pod
metadata:
  name: reject-host-network
  namespace: demo
spec:
  hostNetwork: true
  securityContext:
    runAsUser: 1000
  containers:
  - name: app
    image: nginx:1.27.4
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
