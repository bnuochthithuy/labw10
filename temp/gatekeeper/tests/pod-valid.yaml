apiVersion: v1
kind: Pod
metadata:
  name: pass-valid
  namespace: demo
spec:
  securityContext:
    runAsUser: 1000
  containers:
  - name: app
    image: nginx:1.27.4
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
