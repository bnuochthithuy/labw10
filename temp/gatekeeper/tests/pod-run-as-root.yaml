apiVersion: v1
kind: Pod
metadata:
  name: reject-run-as-root
  namespace: demo
spec:
  securityContext:
    runAsUser: 0
  containers:
  - name: app
    image: nginx:1.27.4
    resources:
      limits:
        cpu: 100m
        memory: 64Mi
